# Maintainer: André Eikmeyer (deqrocks) <dev@deq.rocks>
# Packager: Turann_ <turanull000@gmail.com>

pkgname=t2gmux-dkms
pkgver=0.8.r12.73a1b8e
pkgrel=1
pkgdesc="KaiT2en GMUX module"
arch=('x86_64')
url="https://github.com/kaiT2en/KaiT2en-Fedora"
license=('GPL-2.0-only')
depends=('dkms')
optdepends=('linux' 'linux-headers')
makedepends=('git')
provides=('t2gmux')
conflicts=('linux-t2' 't2gmux')
source=("git+https://github.com/kaiT2en/KaiT2en-Fedora.git")
sha256sums=('SKIP')

pkgver() {
    local src="$srcdir/KaiT2en-Fedora/modules/t2gmux"
    local ver=$(sed -n 's/^[[:space:]]*PACKAGE_VERSION[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' "$src/dkms.conf")
    printf "%s.r%s.%s" "${ver:-0.8}" "$(git -C "$src" rev-list --count HEAD -- .)" "$(git -C "$src" log -1 --format="%h" -- .)"
}

package() {
    local src="$srcdir/KaiT2en-Fedora/modules/t2gmux"
    local dest="$pkgdir/usr/src/t2gmux-$pkgver"
    install -d -m0755 "$dest"
    cp -a "$src/." "$dest/"
    find "$dest" -type f \( -name '*.o' -o -name '*.ko' -o -name '*.ko.zst' -o -name '*.mod' -o -name '*.mod.c' -o -name '*.mod.o' -o -name '.*.cmd' -o -name 'Module.symvers' -o -name 'modules.order' \) -delete
    sed -i "s/^PACKAGE_VERSION=.*/PACKAGE_VERSION=\"$pkgver\"/" "$dest/dkms.conf"
    install -D -m0644 "$startdir/modprobe.d/00-kait2en-t2gmux.conf" "$pkgdir/usr/lib/modprobe.d/00-kait2en-t2gmux.conf"
    install -D -m0644 "$startdir/mkinitcpio.conf.d/00-kait2en-t2gmux.conf" "$pkgdir/etc/mkinitcpio.conf.d/00-kait2en-t2gmux.conf"
    install -D -m0644 "$srcdir/KaiT2en-Fedora/LICENSE" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
